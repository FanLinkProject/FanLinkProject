package org.example.backend.live_session.enums;

/**
 * LIVE / ENDED / RECORDED / READY / REJECTED / EXPIRED
 */
public enum LiveSessionStatus {
	LIVE,
	ENDED,
	RECORDED,
	READY,
	REJECTED,
	EXPIRED
}
